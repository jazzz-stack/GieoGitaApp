// import * as RNlocalize from 'react-native-localize'

// import memoize from 'lodash.memoize';
// import { I18n } from 'i18n-js';
// const translationGetters = {
//   en:{
//     app_title:"Gita jivan git",
//     abhiyaan:"title of target"
// }
// };

// const getLanguage = () => {
//     const fallback = { languageTag: 'en' }; // Default language
//     const { languageTag } = RNlocalize.findBestLanguageTag(Object.keys(translationGetters))
//     return languageTag;
//   };

//   I18n.fallbacks = true;
// I18n.translations = translationGetters;
// I18n.locale = getLanguage();

// export const translate = memoize(
//     (key, config) => I18n.t(key, config), // Use I18n.t here
//     (key, config) => (config ? key + JSON.stringify(config) : key),
//   );
  


